"use client";

import { useEffect } from "react";

// This hook is no longer needed - using native dvh instead
// Keeping file for backward compatibility
export function useKeyboardViewportFix() {
  // Empty hook - functionality moved to CSS using dvh
}
